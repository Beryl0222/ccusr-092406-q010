[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "assistive-device-rental"
version = "0.1.0"
requires-python = ">=3.11"

[tool.setuptools]
py-modules = ["catalog"]
